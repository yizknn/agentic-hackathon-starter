# Shuffle Lunch App Specification (Draft)

## 1. 概要 (Overview)
社内のメンバーを入力すると、3〜4人のランチグループを自動生成し、グループごとに推奨されるランチの行き先（お店やジャンル）を提案するWebアプリケーション。

## 2. ターゲットユーザー & ユースケース
* **ユーザー:** 社内のユーザー全員（全社員が利用可能）
* **ユースケース:**
    1.  参加者リスト（名前）をテキストエリアに貼り付ける。
    2.  「シャッフル」ボタンを押す。
    3.  生成されたグループと行き先を確認し、Slack等で共有する。

## 3. 機能要件 (Functional Requirements)

### 3.1 入力 (Input) - ✅ 実装済み
* **参加者リスト:** テキスト入力。改行区切りまたはカンマ区切りで名前を入力。
* **重複除去:** 同一名の自動除去機能
* **リアルタイム表示:** 参加者数のリアルタイム表示
* **バリデーション:** 3人未満の場合のエラー表示

### 3.2 ロジック (Logic)
#### A. グループ分けアルゴリズム - ✅ 実装済み
* **制約:** 1グループあたり最小3人、最大4人。
* **実装済みロジック:**
    * 4人グループを優先的に作成（効率性重視）
    * Fisher-Yatesシャッフルによる完全ランダム分割
    * 特例処理: 5人の場合は5人1組を許可
* **分割例（実装済み）:**
    * 5人 → 5人1組（特例）
    * 7人 → 4人 + 3人
    * 10人 → 4人 + 3人 + 3人
    * 89人 → 4人×21組 + 5人×1組

#### B. 行き先提案 (Restaurant Suggestion) - ✅ 実装済み
* グループごとに1つの行き先（ジャンル）を提案する。
* **実装済みデータソース:**
    * 15種類のプリセットリスト：中華料理、イタリアン、和食、カレー、ラーメン、ハンバーガー、寿司、タイ料理、フレンチ、カフェ、焼肉、うどん・そば、パスタ、定食、お弁当
    * 各グループにランダムで割り当て

### 3.3 出力・表示 (Output / UI) - ✅ 実装済み
* **カード形式:** グループごとにカードを表示。
    * グループ名（Team A, Team B, Team C...）
    * メンバーリスト（縦並び表示、改行で見やすく）
    * ランチの行き先（ジャンル）
    * トークテーマ（12種類からランダム選択）
* **実装済み機能:**
    * **コピー機能:** 結果をクリップボードにコピー（改行付きテキスト形式）
    * **再シャッフル:** 同じメンバーで再度ランダム分割
    * **レスポンシブ対応:** モバイル1列、タブレット2列、デスクトップ3列表示
    * **CSS Modules:** スタイルの完全分離

## 4. 技術スタック (Tech Stack) - ✅ 実装済み
* **Frontend:** Vue.js 3 + Vite (Composition API, CSS Modules)
* **Backend:** なし（フロントエンドのみのSPA）
* **Deploy:** 静的ホスティング対応（Vercel, Netlify等）

## 5. エッジケース (Edge Cases) - ✅ 実装済み
* **参加者が3人未満の場合:** エラーメッセージを表示、シャッフルボタン無効化
* **参加者が5人の場合:** 特例として5人グループを作成
* **空入力・重複名:** 自動除去とバリデーション

## 6. 実装済み追加機能 (Implemented Additional Features)

### 6.1 トークテーマ機能
* 各グループに会話のきっかけとなるトークテーマを自動提案
* 12種類のテーマ：「最近見たおすすめ映画・ドラマ」「今年やってみたいこと」「好きな旅行先」等

### 6.2 ユーザビリティ向上
* **リアルタイムフィードバック:** 参加者数の即座表示
* **ワンクリック操作:** 結果コピー、再シャッフル、戻る機能
* **視覚的フィードバック:** ホバーエフェクト、グラデーション、アイコン使用

### 6.3 技術的特徴
* **パフォーマンス:** クライアントサイドのみで高速動作
* **モダンフロントエンド:** Vue 3 Composition API, CSS Modules
* **アクセシビリティ:** セマンティックHTML、適切なラベリング

## 7. 将来の拡張性 (Future Works / Nice to have)
* **AI統合:** OpenAI/Claude APIでより賢い行き先提案
* **Slack Bot化:** Slackコマンドでの直接実行
* **Google Maps API連携:** 現在地付近の実際の店舗情報
* **履歴管理:** 「話したことない人同士」を優先するマッチングロジック
* **カスタマイズ:** 食事制限・好み設定機能
* **統計機能:** グループ分け履歴の分析・可視化
