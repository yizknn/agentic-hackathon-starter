# Shuffle Lunch App Specification (Draft)

## 1. 概要 (Overview)
社内のメンバーを入力すると、3〜4人のランチグループを自動生成し、グループごとに推奨されるランチの行き先（お店やジャンル）を提案するWebアプリケーション。

## 2. ターゲットユーザー & ユースケース
* **ユーザー:** 社内のユーザー全員（全社員が利用可能）
* **ユースケース:**
    1.  参加者リスト（名前）をテキストエリアに貼り付ける。
    2.  「シャッフル」ボタンを押す。
    3.  生成されたグループと行き先を確認し、Slack等で共有する。

## 3. 機能要件 (Functional Requirements)

### 3.1 入力 (Input)
* **参加者リスト:** テキスト入力。改行区切りまたはカンマ区切りで名前を入力。
    * *Option:* 部署名や「辛いもの好き」などのタグを入力可能にするか検討（AIマッチング用）。

### 3.2 ロジック (Logic)
#### A. グループ分けアルゴリズム
* **制約:** 1グループあたり最小3人、最大4人。
* **剰余処理:**
    * 合計人数を `N` とする。
    * `N` を3と4の組み合わせで分割する。
    * *例:* 5人の場合 → エラーにするか、特例で5人1組にするか（要相談）。
    * *例:* 7人の場合 → 3人 + 4人。
    * *例:* 10人の場合 → 3人 + 3人 + 4人、または 3人 + 7人(NG) -> 4人 + 3人 + 3人。

#### B. 行き先提案 (Restaurant Suggestion)
* グループごとに1つの行き先（またはジャンル）を提案する。
* **データソース:**
    * MVP: プリセットされたリスト（中華、イタリアン、和食、カレー...）からランダム。
    * **AI Feature:** 参加者の属性（もし入力があれば）や、「今の気分」プロンプトに基づいてLLMが提案。

### 3.3 出力・表示 (Output / UI)
* **カード形式:** グループごとにカードを表示。
    * グループ名（例: Team A, Team Burger 等）
    * メンバーリスト
    * ランチの行き先
    * （AI機能）そのグループ向けの「トークテーマ」を1つ提案。
* **コピー機能:** 結果をクリップボードにコピーできる（Markdown形式またはテキスト形式）。

## 4. 技術スタック (Tech Stack) - ※チームで決定
* **Frontend/Backend:** (例: Streamlit, Next.js + Vercel, Python + FastAPI)
* **AI Model:** (例: OpenAI GPT-4o, Claude 3.5 Sonnet)
* **Deploy:** (例: Vercel, Streamlit Cloud)

## 5. エッジケース (Edge Cases)
* 参加者が3人未満の場合: エラーメッセージを表示。
* 参加者が5人の場合: 特例として5人グループを作るか、エラーとするか。

## 6. 将来の拡張性 (Future Works / Nice to have)
* Slack Bot化。
* Google Maps API連携で現在地付近の店を出す。
* 「話したことない人同士」を優先するマッチングロジック（履歴管理が必要）。
